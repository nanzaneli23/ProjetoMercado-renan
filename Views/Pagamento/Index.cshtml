@model IEnumerable<ProjetoMercado.Models.Pagamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Pagamento</h1>

<p>
    <a asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FormaPagamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FormaPagamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PagamentoId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.PagamentoId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.PagamentoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
